#It is generated by the script with the sourcing file as:1d_fabric_access_polcies.yml
  - name: 1d Add a new AEP for User Tenant
    aci_aep:
      hostname: '{{ inventory_hostname }}'
      username: '{{ username }}'
      password: '{{ password }}'
      validate_certs: no
      aep: '{{ item.aaep }}'
      state: present
    with_items: "{{ aci_aep }}"
    delegate_to: localhost

# the end

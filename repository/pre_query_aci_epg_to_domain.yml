#It is generated by the source task as:task_list.txt
  - name: 3a Map Tenant Phy Domain to new EPGs
    aci_epg_to_domain:
     host: "{{ inventory_hostname }}"
     username: "{{ username }}"
     password: "{{ password }}"
     tenant: "{{ item.tenantName }}"
     ap: "{{ item.apName }}"
     epg: "{{ item.epgName }}"
     domain: "{{ item.domainName }}"
     domain_type: phys
     state: query
     validate_certs: no
    register: query_result
    with_items: "{{ aci_epg_to_domain }}"
    delegate_to: localhost


  - name: Assert the existing object
    assert:
      that:
      - item.current == []
      success_msg: "Object does not exist. Can create it."
      fail_msg: "Object exists. DO NOT recreate it!."
    with_items: "{{query_result.results}}"


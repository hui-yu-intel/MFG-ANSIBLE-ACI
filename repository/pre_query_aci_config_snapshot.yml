#It is generated by the source task as:task_list.txt
  - name: Post Query a Snapshot
    aci_config_snapshot:
     host: '{{ inventory_hostname }}'
     username: '{{ username }}'
     password: '{{ password }}'
     validate_certs: no
     export_policy: config_backup
     max_count: 10
     description: phase 3 completed
     state: query

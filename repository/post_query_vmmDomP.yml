#It is generated by the script with the sourcing file as:vmm_domain.txt
  - name: pb1c Configure VSwitch Policy for VMM Domain with enhanced LAG
    aci_rest:
      host: "{{ inventory_hostname }}"
      username: "{{ username }}"
      password: "{{ password }}"
      validate_certs: no
      path: /api/node/mo/uni/vmmp-VMware/dom-{{ item.vmmDomainName }}.json
      method: get
    register: query_result
    delegate_to: localhost
    with_items: "{{vmmDomP}}"

  - name: Assert the object
    assert: 
      that: 
      - item.imdata!=[]
      success_msg: "Object exists under the tenant."
      fail_msg: "Object does NOT exist."
    with_items: "{{query_result.results}}"
